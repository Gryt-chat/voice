/**
 * @gryt/voice
 *
 * Only the boundary so far. The engine itself is still in the Gryt client and
 * moves across once these interfaces have been agreed — see GRYT-334.
 */
export type {
  AudioPipeline,
  AudioPipelineOptions,
  CameraConstraints,
  RoomAccess,
  RoomCoordinator,
  ScreenConstraints,
  SfuInbound,
  SfuOutbound,
  SfuTransport,
  VoiceConfig,
  VoiceDevice,
  VoiceEngineOptions,
  VoiceLogger,
  VoicePlatform,
} from "./types.js";
